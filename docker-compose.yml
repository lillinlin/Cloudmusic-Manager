services:
  cloudmusic:
    image: ghcr.io/你的GitHub用户名/cloudmusic-dashboard:latest
    container_name: cloudmusic
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
